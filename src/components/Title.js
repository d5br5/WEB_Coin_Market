import React from "react";
import styled from "styled-components";

const Container = styled.div`
  width: 1200px;
  margin: 0 auto 50px;
  text-align: center;
  font-size: 50px;
  font-weight: 600;
`


const Title = () => {
    return <Container>🚀🌙 To the Moon 🌙🚀</Container>
}

export default Title;